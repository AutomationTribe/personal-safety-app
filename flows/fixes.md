SMS notification on signup
sms notification should be sent to the registered sms on successful signup as the user is been navigated to the profile page to subscribe to a plan

Trial card requirement
Trial card should be required when user selects trial, kindly implement

Update subscription flow
After a user selects a subscription plan, the payment interface to select a payment method should be displayed.

Trial checks
if a user has supassed the trial period. that user cannot login with the same credentials to access the app or features


Clicking trip mode user mode in the dashboard should not trigger the add a trip mode form, it should just slide to the trip mode option, activating trip mode and deactivating always on. activating trip mode means no tracking of user until a trip is set.