N/A — subscription flow is mobile only.

The admin web dashboard has no role in the subscription or payment flow.
Paystack webhooks write directly to Supabase profiles via the backend service-role client.
