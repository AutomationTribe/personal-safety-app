# Frontend Dashboard Flow

N/A — the dashboard is mobile only (React Native / Expo).

The web dashboard (dashboard/) is for responders and admin monitoring, not the user-facing
safety dashboard. No changes to the web dashboard are required for this feature.
